# 2026-08-23 — Fase 2: seed inicial aplicado

**Rama:** master
**Commit(s):** _pendiente — se completa al commitear este cambio_

## Qué cambió

Se insertó el catálogo inicial de menús vía `src/migrations/1787536340018-SeedInitialData.ts`:
un menú padre agrupador "Administración" (`key: administration`, sin `path`, sin permiso
propio) y cuatro menús hijos: "Empresas" (`companies`), "Usuarios" (`users`), "Roles"
(`roles`), "Permisos" (`permissions`).

## Motivo

Fase 2 del plan de `auth`. Se define que el nodo padre es puramente organizativo (agrupa en
el sidebar) y no necesita su propia fila en `role_menu_permissions`: el frontend lo muestra
automáticamente si el usuario puede ver al menos uno de sus hijos.

## Qué había antes

No existía ningún menú en la base.
