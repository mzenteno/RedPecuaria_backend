/** Forma mínima para un combo — solo lo que un `<select>` necesita
 * (mostrar y elegir). Igual criterio que `UserOptionResponseDto`. */
export class PropertyOptionResponseDto {
  id: string;
  name: string;
}
