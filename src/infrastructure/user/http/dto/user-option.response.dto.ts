/** Forma mínima para un combo — solo lo que un `<select>` necesita
 * (mostrar y elegir). Igual criterio que `PropertyOptionResponseDto`. */
export class UserOptionResponseDto {
  id: string;
  fullName: string;
}
